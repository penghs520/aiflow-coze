# 后台管理系统前端界面设计文档

## 1. 技术栈与架构

### 1.1 技术选型
| 类别 | 技术方案 | 版本 | 说明 |
|------|---------|------|------|
| **前端框架** | Vue 3 | 3.3+ | 组合式API，更好的TypeScript支持 |
| **UI组件库** | Element Plus | 2.4+ | 基于Vue 3的Element UI重构版 |
| **构建工具** | Vite | 5.0+ | 极速热更新，开发体验优秀 |
| **状态管理** | Pinia | 2.0+ | Vue官方推荐，替代Vuex |
| **路由管理** | Vue Router | 4.0+ | 官方路由解决方案 |
| **HTTP客户端** | Axios | 1.6+ | Promise API，拦截器完善 |
| **代码规范** | ESLint + Prettier | 最新版 | 代码质量和格式统一 |
| **打包分析** | rollup-plugin-visualizer | 最新版 | 包体积分析优化 |

### 1.2 项目结构
```
src/
├── api/                    # API接口定义
│   ├── auth.js           # 认证相关API
│   ├── workflow.js       # 工作流管理API
│   ├── user.js          # 用户管理API
│   ├── task.js          # 任务管理API
│   ├── system.js        # 系统管理API
│   └── index.js         # API统一导出
├── assets/               # 静态资源
│   ├── styles/          # 全局样式
│   │   ├── variables.scss   # 主题变量
│   │   ├── mixins.scss      # SCSS混入
│   │   └── index.scss       # 全局样式入口
│   └── images/          # 图片资源
├── components/          # 公共组件
│   ├── common/         # 通用组件
│   │   ├── SearchBar.vue      # 搜索栏
│   │   ├── DataTable.vue      # 数据表格
│   │   ├── Pagination.vue     # 分页组件
│   │   └── UploadImage.vue    # 图片上传
│   ├── layout/         # 布局组件
│   │   ├── AppLayout.vue      # 应用布局
│   │   ├── Sidebar.vue        # 侧边栏
│   │   ├── Header.vue         # 顶部导航
│   │   └── Breadcrumb.vue     # 面包屑
│   └── business/       # 业务组件
│       ├── workflow/   # 工作流相关
│       └── user/       # 用户相关
├── composables/        # 组合式函数
│   ├── useTable.js    # 表格逻辑复用
│   ├── useForm.js     # 表单逻辑复用
│   └── usePermission.js # 权限检查
├── router/             # 路由配置
│   └── index.js
├── store/              # Pinia状态管理
│   ├── auth.store.js  # 认证状态
│   ├── app.store.js   # 应用状态
│   └── permission.store.js # 权限状态
├── utils/              # 工具函数
│   ├── request.js     # Axios封装
│   ├── auth.js        # 认证工具
│   ├── validate.js    # 表单验证
│   └── date.js        # 日期处理
├── views/              # 页面视图
│   ├── login/         # 登录页
│   ├── dashboard/     # 仪表盘
│   ├── workflow/      # 工作流管理
│   ├── user/          # 用户管理
│   ├── task/          # 任务管理
│   └── system/        # 系统管理
├── App.vue            # 根组件
└── main.js           # 应用入口
```

### 1.3 主题与样式
```scss
// variables.scss
$--color-primary: #409EFF;
$--color-success: #67C23A;
$--color-warning: #E6A23C;
$--color-danger: #F56C6C;
$--color-info: #909399;

// 深色主题（参考即梦App风格）
$--dark-bg: #1a1a1a;
$--dark-text: #ffffff;
$--dark-border: #434343;
$--dark-card: #2d2d2d;
```

## 2. 核心页面设计

### 2.1 登录页 (`/login`)
**设计要点：**
- 简洁专业的登录表单
- 验证码防刷机制
- 深色主题适配

**布局结构：**
```
┌─────────────────────────────────┐
│                                 │
│      [Logo] AI工作流平台        │
│            后台管理系统         │
│                                 │
│    ┌─────────────────────┐     │
│    │ 用户名：____________ │     │
│    │                     │     │
│    │ 密码：______________ │     │
│    │                     │     │
│    │ [验证码] [刷新]     │     │
│    │                     │     │
│    │ [□ 记住我]          │     │
│    │                     │     │
│    │      [登录]         │     │
│    └─────────────────────┘     │
│                                 │
│    © 2026 AI工作流平台         │
└─────────────────────────────────┘
```

**组件配置：**
```vue
<template>
  <div class="login-container">
    <el-card class="login-card">
      <div class="logo-section">
        <img src="@/assets/logo.png" alt="Logo">
        <h2>AI工作流平台</h2>
        <p>后台管理系统</p>
      </div>
      
      <el-form ref="formRef" :model="form" :rules="rules">
        <el-form-item prop="username">
          <el-input
            v-model="form.username"
            placeholder="请输入用户名"
            prefix-icon="User"
            size="large"
          />
        </el-form-item>
        
        <el-form-item prop="password">
          <el-input
            v-model="form.password"
            type="password"
            placeholder="请输入密码"
            prefix-icon="Lock"
            size="large"
            show-password
          />
        </el-form-item>
        
        <el-form-item prop="captcha" v-if="showCaptcha">
          <div class="captcha-wrapper">
            <el-input
              v-model="form.captcha"
              placeholder="请输入验证码"
              prefix-icon="Key"
              size="large"
            />
            <img 
              :src="captchaUrl" 
              @click="refreshCaptcha"
              class="captcha-image"
              alt="验证码"
            />
          </div>
        </el-form-item>
        
        <el-form-item>
          <el-checkbox v-model="form.remember">记住我</el-checkbox>
        </el-form-item>
        
        <el-form-item>
          <el-button
            type="primary"
            size="large"
            :loading="loading"
            @click="handleLogin"
            style="width: 100%"
          >
            登录
          </el-button>
        </el-form-item>
      </el-form>
    </el-card>
  </div>
</template>
```

### 2.2 主布局框架 (`AppLayout.vue`)
**设计要点：**
- 经典后台管理系统布局：左侧菜单 + 顶部导航 + 内容区
- 响应式设计：支持移动端折叠菜单
- 权限控制：动态菜单渲染

**布局结构：**
```
┌─────────────────────────────────────────────┐
│  ┌─────┐ AI工作流平台后台       [用户头像] │
│  │Logo │                                    │
│  └─────┘                                    │
├─────────────────────────────────────────────┤
│                                              │
│  ┌─────────────┐  ┌──────────────────────┐  │
│  │             │  │                      │  │
│  │             │  │                      │  │
│  │  侧边菜单   │  │      内容区域        │  │
│  │             │  │                      │  │
│  │             │  │                      │  │
│  │             │  │                      │  │
│  └─────────────┘  └──────────────────────┘  │
│                                              │
└─────────────────────────────────────────────┘
```

**组件配置：**
```vue
<template>
  <div class="app-wrapper">
    <!-- 侧边栏 -->
    <Sidebar 
      :collapse="sidebarCollapse"
      :menu-list="permissionRoutes"
    />
    
    <!-- 主容器 -->
    <div class="main-container" :class="{ 'sidebar-collapsed': sidebarCollapse }">
      <!-- 顶部导航 -->
      <Header 
        @toggle-sidebar="toggleSidebar"
        :user-info="userInfo"
      />
      
      <!-- 面包屑 -->
      <Breadcrumb />
      
      <!-- 页面内容 -->
      <div class="app-main">
        <router-view v-slot="{ Component }">
          <transition name="fade-transform" mode="out-in">
            <component :is="Component" />
          </transition>
        </router-view>
      </div>
      
      <!-- 页脚 -->
      <Footer />
    </div>
  </div>
</template>
```

### 2.3 仪表盘 (`/dashboard`)
**设计要点：**
- 数据概览卡片
- 关键指标图表
- 实时监控面板

**布局模块：**
1. **统计卡片区**：平台总用户、今日新增、总任务数、成功率
2. **趋势图表区**：任务增长趋势、用户活跃度、资源点消耗
3. **实时监控区**：当前在线用户、正在处理任务、系统负载
4. **快捷操作区**：常用功能入口

**组件配置：**
```vue
<template>
  <div class="dashboard-container">
    <!-- 统计卡片 -->
    <el-row :gutter="20">
      <el-col :xs="24" :sm="12" :md="6">
        <StatCard
          title="总用户数"
          :value="stats.totalUsers"
          icon="User"
          color="#409EFF"
          trend="+12%"
        />
      </el-col>
      <!-- 其他卡片 -->
    </el-row>
    
    <!-- 图表区 -->
    <el-row :gutter="20" class="chart-row">
      <el-col :xs="24" :md="12">
        <LineChart :data="taskTrendData" title="任务增长趋势" />
      </el-col>
      <el-col :xs="24" :md="12">
        <PieChart :data="workflowDistribution" title="工作流使用分布" />
      </el-col>
    </el-row>
    
    <!-- 实时监控 -->
    <el-card class="monitor-card">
      <template #header>
        <div class="card-header">
          <span>实时监控</span>
          <el-tag type="success">运行正常</el-tag>
        </div>
      </template>
      <MonitorPanel />
    </el-card>
  </div>
</template>
```

### 2.4 工作流管理 (`/workflow`)
**设计要点：**
- 列表+详情+表单三合一视图
- 批量操作支持
- 实时状态切换

**页面结构：**
```
工作流管理
├── 搜索筛选区（分类、状态、关键词）
├── 操作按钮区（新增、批量、导出）
├── 数据表格区（列表展示）
├── 详情抽屉（右侧滑出）
└── 表单弹窗（新增/编辑）
```

**表格列设计：**
| 列名 | 字段 | 宽度 | 说明 |
|------|------|------|------|
| 选择 | selection | 55px | 多选复选框 |
| ID | id | 120px | 工作流唯一标识 |
| 名称 | name | 200px | 工作流名称 |
| 分类 | category | 100px | 分类标签 |
| 状态 | status | 100px | 状态标签（上架/下架） |
| 使用次数 | usageCount | 120px | 数字，可排序 |
| 评分 | averageRating | 100px | 星级显示 |
| 创建时间 | createdAt | 180px | 日期时间 |
| 操作 | actions | 180px | 编辑、上架/下架、删除 |

**表单设计：**
```vue
<template>
  <el-dialog :title="dialogTitle" v-model="dialogVisible" width="800px">
    <el-form ref="formRef" :model="form" :rules="rules" label-width="120px">
      <!-- 基础信息 -->
      <el-form-item label="工作流名称" prop="name">
        <el-input v-model="form.name" placeholder="请输入工作流名称" />
      </el-form-item>
      
      <el-form-item label="分类" prop="category">
        <el-select v-model="form.category">
          <el-option label="视频创作" value="video_creation" />
          <el-option label="图文生成" value="image_creation" />
          <el-option label="音频处理" value="audio_processing" />
        </el-select>
      </el-form-item>
      
      <!-- 参数定义（动态表单） -->
      <el-form-item label="参数配置">
        <ParameterEditor v-model="form.parameterDefinition" />
      </el-form-item>
      
      <!-- 资源点设置 -->
      <el-form-item label="基础消耗点数" prop="basePoints">
        <el-input-number v-model="form.basePoints" :min="0" :step="100" />
      </el-form-item>
    </el-form>
    
    <template #footer>
      <span class="dialog-footer">
        <el-button @click="dialogVisible = false">取消</el-button>
        <el-button type="primary" @click="handleSubmit">确定</el-button>
      </span>
    </template>
  </el-dialog>
</template>
```

### 2.5 用户管理 (`/user`)
**设计要点：**
- 用户列表与详情
- 状态管理（启用/禁用）
- 资源点调整

**页面模块：**
1. **用户列表**：分页展示，支持多种筛选条件
2. **用户详情**：完整用户信息展示
3. **状态管理**：批量启用/禁用操作
4. **资源点调整**：手动增减用户资源点

**表格操作：**
- 查看详情（抽屉形式）
- 启用/禁用（状态切换）
- 调整资源点（弹窗表单）
- 登录日志（跳转页面）

### 2.6 任务监控 (`/task`)
**设计要点：**
- 实时任务状态监控
- 失败任务重试
- 执行日志查看

**功能模块：**
1. **任务状态看板**：各状态任务数量统计
2. **实时任务列表**：最新任务动态
3. **失败任务处理**：批量重试或手动处理
4. **执行日志查询**：详细的任务执行记录

### 2.7 系统管理 (`/system`)
**子模块：**
1. **角色管理**：角色CRUD，权限分配
2. **操作日志**：管理员操作审计
3. **系统配置**：平台参数设置
4. **通知公告**：系统公告管理

## 3. 核心组件设计

### 3.1 数据表格组件 (`DataTable.vue`)
**功能特性：**
- 支持分页、排序、筛选
- 自定义列渲染
- 批量操作支持
- 导出功能集成

**Props定义：**
```javascript
const props = defineProps({
  // 数据相关
  data: { type: Array, default: () => [] },
  total: { type: Number, default: 0 },
  loading: { type: Boolean, default: false },
  
  // 列配置
  columns: { type: Array, required: true },
  
  // 分页配置
  pagination: { 
    type: Object, 
    default: () => ({ 
      page: 1, 
      size: 20,
      sizes: [10, 20, 50, 100] 
    }) 
  },
  
  // 功能开关
  selection: { type: Boolean, default: false }, // 是否显示选择列
  index: { type: Boolean, default: false },     // 是否显示序号列
  border: { type: Boolean, default: true },     // 是否显示边框
  stripe: { type: Boolean, default: true },     // 是否显示斑马纹
});
```

### 3.2 搜索栏组件 (`SearchBar.vue`)
**功能特性：**
- 多条件组合搜索
- 快捷筛选标签
- 搜索历史记录

**布局结构：**
```
┌──────────────────────────────────────┐
│ [搜索框] [分类筛选] [状态筛选] [搜索] │
│ [标签1] [标签2] [标签3] [清空筛选]    │
└──────────────────────────────────────┘
```

### 3.3 图片上传组件 (`UploadImage.vue`)
**功能特性：**
- 拖拽上传支持
- 图片预览与裁剪
- 多格式支持（JPG、PNG、WebP）
- 大小限制验证

## 4. 交互与状态管理

### 4.1 权限控制实现
```javascript
// usePermission.js 组合式函数
export function usePermission() {
  const route = useRoute();
  const permissionStore = usePermissionStore();
  
  // 检查是否有权限
  const hasPermission = (permissionCode) => {
    if (!permissionCode) return true;
    const permissions = permissionStore.permissions;
    return permissions.includes('*:*') || permissions.includes(permissionCode);
  };
  
  // 路由守卫检查
  const checkRoutePermission = (to) => {
    const requiredPermission = to.meta?.permission;
    if (requiredPermission && !hasPermission(requiredPermission)) {
      return '/403'; // 无权限页面
    }
    return true;
  };
  
  return { hasPermission, checkRoutePermission };
}
```

### 4.2 表单验证规则
```javascript
// validate.js
export const formRules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' },
    { min: 3, max: 20, message: '长度在 3 到 20 个字符', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 6, max: 20, message: '长度在 6 到 20 个字符', trigger: 'blur' },
    { pattern: /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).+$/, 
      message: '必须包含大小写字母和数字', trigger: 'blur' }
  ],
  phone: [
    { pattern: /^1[3-9]\d{9}$/, 
      message: '请输入正确的手机号码', trigger: 'blur' }
  ]
};
```

### 4.3 请求拦截器配置
```javascript
// request.js
const service = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 10000
});

// 请求拦截器
service.interceptors.request.use(
  config => {
    const token = localStorage.getItem('admin_token');
    if (token) {
      config.headers['Authorization'] = `Bearer ${token}`;
    }
    return config;
  },
  error => {
    return Promise.reject(error);
  }
);

// 响应拦截器
service.interceptors.response.use(
  response => {
    const res = response.data;
    if (res.code === 200) {
      return res.data;
    } else {
      // 业务错误处理
      ElMessage.error(res.message || '请求失败');
      return Promise.reject(new Error(res.message || 'Error'));
    }
  },
  error => {
    // HTTP错误处理
    if (error.response?.status === 401) {
      // Token过期，跳转登录
      router.push('/login');
    }
    ElMessage.error(error.message || '请求失败');
    return Promise.reject(error);
  }
);
```

## 5. 响应式设计与适配

### 5.1 断点配置
```scss
// 响应式断点（与Element Plus保持一致）
$--sm: 768px;
$--md: 992px;
$--lg: 1200px;
$--xl: 1920px;
```

### 5.2 布局适配策略
1. **桌面端（≥992px）**：完整侧边栏，全功能展示
2. **平板端（768px-991px）**：侧边栏折叠，主要功能可用
3. **移动端（<768px）**：顶部导航主导，精简功能

### 5.3 组件响应式示例
```vue
<template>
  <el-row :gutter="20">
    <!-- 不同屏幕尺寸下的列宽分配 -->
    <el-col :xs="24" :sm="12" :md="8" :lg="6">
      <StatCard />
    </el-col>
  </el-row>
</template>
```

## 6. 性能优化策略

### 6.1 代码分割与懒加载
```javascript
// 路由懒加载
const WorkflowList = () => import('@/views/workflow/List.vue');
const WorkflowDetail = () => import('@/views/workflow/Detail.vue');

// 组件懒加载
const ChartComponent = defineAsyncComponent(() =>
  import('@/components/Chart.vue')
);
```

### 6.2 请求优化
1. **防抖搜索**：输入框搜索添加防抖处理
2. **请求缓存**：频繁请求的数据添加内存缓存
3. **批量操作**：支持批量处理，减少请求次数

### 6.3 渲染优化
1. **虚拟滚动**：大数据量列表使用虚拟滚动
2. **图片懒加载**：图片资源延迟加载
3. **组件按需引入**：Element Plus组件按需引入

## 7. 开发与部署

### 7.1 开发环境配置
```javascript
// .env.development
VITE_API_BASE_URL=http://localhost:8080/api/admin/v1
VITE_APP_TITLE=AI工作流平台后台（开发环境）
```

### 7.2 构建配置
```javascript
// vite.config.js
export default defineConfig({
  plugins: [
    vue(),
    // Element Plus按需引入
    AutoImport({
      resolvers: [ElementPlusResolver()],
    }),
    Components({
      resolvers: [ElementPlusResolver()],
    }),
  ],
  build: {
    // 分包策略
    rollupOptions: {
      output: {
        chunkFileNames: 'static/js/[name]-[hash].js',
        entryFileNames: 'static/js/[name]-[hash].js',
        assetFileNames: 'static/[ext]/[name]-[hash].[ext]',
      }
    }
  }
});
```

### 7.3 部署脚本
```bash
#!/bin/bash
# deploy.sh

# 构建
npm run build

# 上传到服务器
scp -r dist/* user@server:/var/www/admin/

# 重启nginx
ssh user@server "sudo systemctl reload nginx"
```

---

**设计规范总结**
1. **一致性原则**：所有页面保持统一的风格和交互逻辑
2. **可用性原则**：操作流程清晰，减少用户学习成本
3. **反馈原则**：用户操作后及时给予明确反馈
4. **容错原则**：提供撤销操作和错误恢复机制
5. **效率原则**：常用操作提供快捷入口，减少操作步骤

**下一步开发重点**
1. 搭建基础框架和路由配置
2. 实现权限控制体系
3. 开发通用组件库
4. 对接后端API接口
5. 进行全面的测试和优化